{
  "name": "web config round-trip: set a value via the form and show it",
  "platform": "web",
  "note": "driven by the agent through the Browser MCP against python -m jarvis.web",
  "steps": [
    {"action": "fill cfg-key=temperature, cfg-value=0.7, click Set", "expect": "temperature = 0.7"},
    {"action": "click Show config", "expect": "temperature"}
  ]
}
